---
import { Image } from 'astro:assets';
import logoWhite from '~/assets/logo/logo-white.png';
import logo from '~/assets/logo/logo.jpg';
import CursorToggle from '~/components/CursorToggle.astro';
import MobileMenu from '~/components/MobileMenu.astro';
import ThemeToggle from '~/components/ThemeToggle.astro';
import { site } from '~/lib/site';

const path = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<header
  id="site-header"
  class="sticky top-0 z-40 backdrop-blur-md bg-[color-mix(in_oklab,var(--color-bg)_80%,transparent)] border-b border-border"
>
  <div class="container-page flex items-center justify-between py-3">
    <a href="/" class="flex items-center gap-3" aria-label={`${site.name} — strona główna`}>
      <Image src={logo} alt={site.name} width={70} height={70} class="rounded-full object-cover light:hidden" />
      <Image src={logoWhite} alt={site.name} width={70} height={70} class="hidden rounded-full object-cover light:block" />
    </a>

    <nav aria-label="Główna nawigacja" class="hidden md:flex items-center gap-1 md:gap-2">
      {site.nav.map((item) => {
        const active = item.href === '/' ? path === '/' : path.startsWith(item.href);
        return (
          <a
            href={item.href}
            aria-current={active ? 'page' : undefined}
            class:list={[
              'px-3 py-2 rounded-full text-sm transition-colors',
              active ? 'text-brand-400' : 'text-fg-muted hover:text-fg',
            ]}
          >
            {item.label}
          </a>
        );
      })}
      <CursorToggle />
      <ThemeToggle />
    </nav>

    <div class="flex md:hidden items-center gap-2">
      <ThemeToggle />
      <button
        id="mobile-menu-toggle"
        type="button"
        class="nav-burger"
        aria-label="Menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <span class="nav-burger-line"></span>
        <span class="nav-burger-line"></span>
        <span class="nav-burger-line"></span>
      </button>
    </div>
  </div>

  <MobileMenu />
</header>
